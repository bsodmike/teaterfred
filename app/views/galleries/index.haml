%h3 Galleri
%table
  - @galleries.each do |gallery|
    %tr
      %td{:colspan => '4'}
        %h4= link_to gallery.title, gallery
        = gallery.place
        %br/
        = gallery.created_at
    %tr
    %tr
      - gallery.images.each do |image|
        %td= image_tag image.image, :width => 100, :height => 100, :class => "image_box"
        
    %tr
      %td

        - if current_user
          = link_to 'Vis', gallery
          = link_to 'Rediger', edit_gallery_path(gallery)
          = link_to 'Slet', gallery, confirm: 'Er du sikker?', method: :delete
    


  